const defaultCode = `
// A monaco code editor for expressions
// with docs and autocompletion

// Hover over values to see the docs!
const wiggled = thisComp.layer("layer name").transform.position.wiggle(1, 10);
`;

export default defaultCode;
